:aws:
  :access_key_id: ADD HERE
  :secret_access_key: ADD HERE
:s3:
  :region: ADD HERE # For Redshift, S3 bucket region must be the same as Redshift cluster region
  :buckets:
    :in: ADD HERE
    :archive: ADD HERE
:download:
  :folder: ADD HERE # Postgres-only config option. Where to store the downloaded files. Leave blank for Redshift
# Currently assumes we are loading only one target
:storage:
  :type: redshift # Or 'postgres'
  :host: ADD HERE # For Redshift, the endpoint as shown in the Redshift console. For Postgres, the hostname of your database server
  :database: ADD HERE # Name of database 
  :port: 5439 # The Redshift default port, or '5432' as the Postgres default port
  :table: events # For Redshift, or 'atomic.events' for Postgres
  :username: ADD HERE 
  :password: ADD HERE 