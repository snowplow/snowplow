/**
 * Custom Unstructured Event event_type
 */

@namespace("com.snowplowanalytics.snowplow.storage.avro")
protocol CustomStructuredEventProtocol {

	import idl "../../simple_types/Properties.avdl";

	record CustomStructuredEvent {
		string name;
		union{map<Properties>, null} Properties;
	}
}